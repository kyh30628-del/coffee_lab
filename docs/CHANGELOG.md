# 변경 기록 (CHANGELOG)

> 동네 커피 노트의 주요 변경 이력. 최신이 위. 원칙은 `PRINCIPLES.md`·`STATUS.md`.

---

## 2026-06-07 — 해자 강화 · 유튜브 추가 · 비용 안전화

### 1. 리뷰 검증 엔진 — Sonnet을 '최종 심사관'으로 (핵심 해자)
- **2단 검증 확립**: 규칙 엔진(1차 노이즈 제거) → **Claude Sonnet**이 on-topic 후보 전체를 최종 심사.
  - 양방향 정밀도: 놓친 진짜 후기 **구제** + 규칙이 잘못 통과시킨 **거짓양성 제거**(예: '가는 길·주차' 정보글).
  - 판정 기준: `about`(실제 그 카페 방문) × `helpful`(구체 경험) 둘 다 참만 통과, 애매하면 보수적 제외.
- **Max 구독으로 무료 실행**: 판정은 실시간이 아닌 **배치** → 사장님 맥에서 Claude Max로 매일 04:00(launchd). 웹앱(Vercel)은 LLM 호출 0, 결과만 서빙. 무회귀(토큰 실패 시 규칙 결과 유지).
- **✨ AI 검증 배지**: Sonnet 통과 후기를 소비자 화면에 노출(차별점 가시화).
- 관리자 대시보드에 **엔진 현황**(원본 수집%·AI 판정%·노이즈 제거율) + 공개 `engine-status` API.

### 2. 쿼터 최적화 · 안정성
- **raw 원본 보존**(`raw_reviews`): 한 번 수집한 후기를 저장 → 재합성·재판정 시 API 재호출 0(쿼터 절약·재현).
- **예열(warmup)**: 매일 00:10(launchd) raw 없는 카페만 수집(populate 모드), 쿼터 소진 시 자동 중단.
- **네이버 display 20→100**(호출 수 동일=쿼터 동일, 후기 ~5배).
- **데이터 파괴 방지**: 쿼터/오류로 수집 0이면 '진짜 0건'과 구분해 DB 미변경(보존).

### 3. 검색 강화
- **Claude Sonnet 맥락 재정렬**: 임베딩으로 후보 압축 → Sonnet이 질문 의도로 선별·정렬(콘솔 키 시). 키 없으면 임베딩 폴백(무회귀).
- **결과 캐시**(질문+지역, 12h): 같은·비슷한 질문은 재계산 0 → 비용은 "질문 종류 수"만큼만.

### 4. 유튜브 수집기 추가 (신규 합법 소스)
- **YouTube Data API v3**로 카페 영상 검색 → 제목·설명·상위 댓글 + 영상 링크·게시일 수집.
- 다른 소스와 **동일한 검증 엔진**(주제성·동명·지역 필터)+Sonnet 판정 통과분만 채택, 합성 가중 1.1.
- 카페 상세에 **▶YouTube 배지 + 영상 보기 링크**(사실성). 자막/원문은 합법 불가 → 제목·설명·댓글까지만.
- 실측: 프릳츠 도화점 8영상→검증 4, 어니언 성수 8→검증 2 등 정상 작동.

### 5. 랜딩 페이지 개편
- **초기 화면 = 랜딩**: 소비자/사장님 분리. 사장님은 관리자 비밀번호 인증(세션). `/owner` 자체 게이트 제거.
- 해자 카피("별점 말고 검증된 후기로 고르세요"), **골드 샤이머 헤드라인 + 등장 애니메이션 + 김 모락모락**.
- 홈 좌측상단 타이틀 클릭 → 랜딩. 뒤로가기: 홈→랜딩, 랜딩에서 한 번 더→종료.

### 6. 위치 · 모바일 UX
- **'내 위치' 버튼**: 누르면 동의→현재 위치 새로 수집(캐시 안 함, 재접속 시 재수집).
- 사장님 분석 검색바 모바일 최적화.

### 7. 비용 안전화 (긴급)
- **구글 Places API 기본 OFF**: 호출당 과금(무료크레딧 ₩40만 소진 원인) → 단일 게이트로 synth·스냅샷·auto-synth의 모든 Places 호출 차단. 네이버·유튜브·블로그로 대체.
- **모멘텀 스냅샷을 소유 데이터로**: 외부 평점(미제공) 대신 검증 리뷰 수 + 최근 90일 버즈 추이 기록(합법·무과금).
- (운영 메모: Google Cloud 결제 계정 폐쇄 → 과금 구조적 불가. YouTube·Gemini는 무료 한도로 정상 작동 확인.)

### 8. 발굴 보강 · 기타
- **공공데이터 상가정보(data.go.kr)** 발굴 모듈(누락 최소화, 위경도 직접). 키 필요.
- **서비스 종합 가이드 PDF/HTML** 제작·갱신. DATA_PIPELINE 문서 갱신.

> 자동화 스케줄(현재): 로컬 — 예열 00:10·Sonnet판정 04:00 / Vercel — 발굴+합성 03:00·임베딩 07:00·재수집 월 13:00·스냅샷 일 13:00 (KST).
