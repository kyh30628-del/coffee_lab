# 동네 커피 노트 — 작업 원칙 (HEADER LAW)
> 이 문서는 모든 작업에 절대적으로 우선한다. 코드 한 줄, 데이터 한 건도 이 원칙을 어기면 안 된다.
> 작업 시작 전 항상 이 파일을 먼저 읽고, 모든 결정이 여기에 부합하는지 확인한다.

## 0. 제품 정체성
- "커피를 아는 사람이 안내하는, 취향 기반 동네 카페 가이드".
- 별점 나열(카카오맵·블로그)이 아니라 **교차검증된 사실 + 취향 매칭 + 안목**으로 차별화.
- 시작은 강동·구리(생활권), 목표는 전국. 단 "양"이 아니라 "사실 기반 깊이"로 확장.

## 1. 환각 절대 금지 / 사실 기반 (최상위)
- 지어낸 정보는 제품의 죽음. 한 줄도 환각 금지.
- 모든 카페 정보는 추적 가능한 출처가 있어야 한다.
- 근거 없으면 "데이터 부족"이라 정직히 표시. 메우지 않는다.
- 출력에 근거(리뷰 N건 중 X회 언급)를 함께 보존·표시.
- 2개+ 독립 소스에서 반복된 신호만 채택. 단일/광고성은 버린다.

## 2. 합법성 준수 (타협 불가)
- 약관·저작권 위반 수집(스크래핑 차단 우회 등) 금지.
- 합법 소스만: 구글 Places API, 공개 웹 검색 인덱스, 카페 자발 공개 채널, 사장님 직접 등록.
- 사진 등 저작물은 직접 촬영/권리자 업로드만. 외부 이미지 무단 사용 금지.
- 개인정보 수집·표시 금지.

## 3. 데이터 기반 품질 극대화 (손 놓지 않는다)
- 기술·약관 한계는 포기 사유가 아니라 디벨롭 과제. 문제를 정의하고 개선.
- 한 소스가 막히면 다른 합법 소스로 메운다(다중 소스 합성).
- 신뢰 등급: 검증(리뷰 30+) / 참고(5~30) / 발굴(5 미만).
- 리뷰 적은 "숨은 집"은 약점이 아니라 희소가치·사장님 등록 1순위로 전환.

## 4. 최신성 우선 (NEW — 데이터 수집의 핵심 기준)
- 데이터는 **최신일수록 유의미**하다. 카페는 원두·메뉴·사장·분위기가 자주 바뀐다.
- 수집 시 가능한 한 날짜를 확보하고, **최근 것에 가중치**를 둔다(오래된 신호는 신뢰도↓).
- 검색·재수집은 최신 기준으로 갱신하며, 오래된 정보는 "수집 시점"을 함께 표시한다.
- 폐업·이전·원두 변경 등 변화 신호가 보이면 옛 데이터보다 우선 반영한다.
- 정기 재수집(갱신) 구조를 전제로 설계한다.

## 5. 종합적 사고 / 틀을 깨는 유연함
- "사람이 발로 뛰어 채운다"는 전통적 틀에 갇히지 않는다.
- AI의 강점(대량 데이터 통합·패턴 추출)으로 사람 한 명이 불가능한 방식으로 푼다.
- 매 작업을 전체 그림 안에서 판단. 안전한 정답 반복으로 회피하지 않는다.

## 6. 작업 방식
- 코드는 항상 전체 파일로 제공(부분 수정 지양).
- 새 API 라우트: 폴더 생성 확인 → 파일 생성 → dev 재시작 순서 준수.
- 큰 작업은 단계로 쪼개되, 시작한 방향(A는 B까지)을 끝까지 잊지 않는다.

## 7. 합성 엔진 — 확정된 설계·과제
- 신호 추출: 키워드 + 부정맥락 보정(다국어) + 모호어(smooth/clean=바디↓) 처리.
- 좌표화: 2건+ 교차검증된 축만. 부족하면 None(지어내지 않음).
- 신뢰등급·근거 배지 보존.
- **[과제] 검색 노이즈 필터**: 웹검색 결과가 "그 카페 리뷰"인지 검증(카페명+지역 본문 포함, 리뷰 맥락) 후에만 사용. 일반 교양글·동명 카페 배제.
- **[과제] 최신성 가중**: 리뷰에 날짜 있으면 최근 것 가중, 오래된 것 감쇠.

## 8. 현재 로드맵
- [진행] 합성 엔진을 사용자 환경(/api)에 연결 → 화면 표시
- [다음] B단계: 노이즈 필터 + 다중소스(구글+웹검색) 합성 + 최신성 가중
- 사장님 등록(발굴 대상 우선) / 사용자 기여
- 검증된 모델을 옆 동네로 복제(전국은 결과)
