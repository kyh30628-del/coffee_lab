# 복사해서 scripts/.judge.env 로 저장하고 값 채우기 (이 파일은 커밋 금지).
# Claude Max 구독 토큰: 터미널에서 `claude setup-token` 실행 후 출력값
export CLAUDE_CODE_OAUTH_TOKEN=""
export APP_URL="https://coffee-lab-product-builder.vercel.app"
export ADMIN_PASSWORD=""
# 선택: 모델·1회 상한
export JUDGE_MODEL="claude-sonnet-4-5"
export JUDGE_MAX="400"
