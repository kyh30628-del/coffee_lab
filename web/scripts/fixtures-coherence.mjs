#!/usr/bin/env node
// 🛡️ 이름 일관성(공개 게이트) 위반 픽스처 — 실행: node --import tsx scripts/fixtures-coherence.mjs
//
//   이 게이트가 후기 하나하나에 "이거 정말 그 카페 얘기냐"를 묻는다. 여기가 틀리면 두 방향으로 다 망한다:
//     ① 진짜를 오염으로 버리면 → coherence가 떨어져 공개 게이트(0.55)에 걸려 **영영 공개 안 된다**
//     ② 오염을 진짜로 통과시키면 → 남의 가게 후기가 우리 화면에 뜬다(해자 붕괴)
//   2026-09-18 실측: 승격대기 181곳이 ①로 막혀 있었다. 아래 MUST_HIT는 전부 그때 실제로 버려지던 진짜 후기다.
const { nameCoherence } = await import("../lib/reviewQuality.ts");

// [카페명, 후기, 히트여야 하나, areaTerms, 출처]
// [카페명, 후기, 히트여야 하나, areaTerms, 출처, 주소(선택)]
const CASES = [
  // ── ③ 주소 신호(2026-09-20) — 상호가 없어도 도로명+번지가 맞으면 그 카페다 ──
  ["카페이면", "제주시 한림읍 금능 5길 13 1층 월-금 10:00 ~ 15:00 매주 토, 일 정기휴무", true, ["제주시", "한림읍"], "주소·영업시간만 적힌 진짜 후기", "제주특별자치도 제주시 한림읍 금능5길 13 1층"],
  ["카페이면", "제주시 한림읍에 있는 다른 카페 다녀왔어요 분위기 좋더라구요", false, ["제주시", "한림읍"], "동만 일치하는 옆가게 — 통과시키면 안 된다", "제주특별자치도 제주시 한림읍 금능5길 13 1층"],
  ["카페이면", "오늘 날씨가 좋아서 산책을 했다 기분이 좋았다", false, ["제주시", "한림읍"], "무관 글", "제주특별자치도 제주시 한림읍 금능5길 13 1층"],

  // ── ① 진짜인데 버려지던 것(2026-09-18 실측, 승격대기 카페에서 채취) ──
  ["민들레 카페", "여주점동 레트로분위기 가득한 카페민들레 여주시 점동면 청안리길 2 Open 오전 9시", true, ["여주시", "점동면"], "id? 업종어가 상호 앞"],
  ["민들레 카페", "아기자기 소담한 카페 민들레 여주시 점동 핫플에 위치한 민들레카페 소개합니다", true, ["여주시", "점동면"], "정상 표기"],
  ["베이커 바미 군포삼성마을점", "군포 베이커바미 후기 공갈빵이랑 소금빵 추천, 가성비 빵 맛집", true, ["군포시", "부곡동"], "띄어쓰기 변형"],
  ["베이커 바미 군포삼성마을점", "군포 삼성마을 베이커리 맛집 베이커바미 제가 요즘 폭 빠져있는", true, ["군포시", "부곡동"], "띄어쓰기 변형"],
  ["베이커 바미 군포삼성마을점", "[군포] 삼성마을 빵 카페 '베이커 바미' 숨은 맛집을 찾은 것 같아서", true, ["군포시", "부곡동"], "정상 표기"],

  // ── ② 오염이라 반드시 막아야 하는 것(과거 실사고 — 완화가 이걸 뚫으면 안 된다) ──
  // ⚠️ 접두 결합("두레커피마을" ⊃ "커피마을")은 **이 함수의 책임이 아니다** — 수집 단계의
  //   quoteMatchConfidence(attachedNamePrefix)가 앞에서 거른다. 여기 HIT는 원본부터의 동작이고,
  //   2026-09-18 수정과 무관하다(원본 대조로 확인). 층이 바뀌면 이 줄이 먼저 깨지라고 남긴다.
  ["커피마을", "두레커피마을 다녀왔어요 여기 원두가 참 좋더라구요 분위기도 아늑하고", true, ["성남시", "분당동"], "다층 방어: 여기 아닌 수집단계가 담당"],
  ["외할머니", "카페휴 다녀왔습니다 조용하고 좋네요 커피도 맛있어요", false, ["서울", "마포구"], "id15311 까페 외할머니 오염"],
  ["라온", "라온킥복싱 등록했어요 관장님이 친절하시고 시설도 깨끗합니다", false, ["서울", "강남구"], "동명 타업종"],
  ["코르누코피아 뱅뱅사거리점", "자연곳간 뱅뱅사거리점 점심 먹었는데 반찬이 깔끔하네요", false, ["서울", "서초구"], "지점 랜드마크 공유(coord#429)"],
  ["신상", "오늘 신상 원피스를 샀는데 색이 예뻐요 다음에 또 사야지", false, ["서울", "중구"], "일반어 상호(coord#230)"],
];

let pass = 0, fail = 0;
for (const [name, quote, want, area, src, addr] of CASES) {
  const got = nameCoherence(name, [quote], area, addr) === 1;
  if (got === want) { pass++; }
  else { fail++; console.log(`  ❌ [${name}] 기대 ${want ? "HIT" : "MISS"} → 실제 ${got ? "HIT" : "MISS"}  (${src})\n     "${quote.slice(0, 60)}"`); }
}
console.log(`\n이름 일관성 픽스처: ${pass}/${CASES.length} 통과${fail ? ` · ${fail}건 실패` : ""}`);
process.exit(fail ? 1 : 0);
